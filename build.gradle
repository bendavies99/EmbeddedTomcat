plugins {
    id 'base'
    id 'maven-publish'
}

allprojects {
    group 'net.bdavies.embedded-tomcat'
    version '0.0.1-SNAPSHOT'
}

subprojects {
    afterEvaluate {
        def compat = JavaVersion.VERSION_11;
        sourceCompatibility = compat;
        targetCompatibility = compat;

        repositories {
            mavenCentral()
            mavenLocal()
        }
    }
}
